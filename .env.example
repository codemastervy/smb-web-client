# Copy to .env and edit before the first start.

# Required. Gates the web UI AND encrypts saved SMB passwords.
# Changing it later makes saved SMB passwords undecryptable (they are dropped
# and must be re-entered) -- see README > Security model.
#   openssl rand -base64 24
ADMIN_PASSWORD=change-me-before-first-start

TZ=Pacific/Auckland
